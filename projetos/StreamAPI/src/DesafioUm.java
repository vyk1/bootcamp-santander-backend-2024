public class DesafioUm {

    public static void main(String[] args) {
        // Crie um array de inteiros e ordene o array
        int[] numeros = { 1, 5, 3, 2, 4 };
        Arrays.sort(numeros);
        System.out.println(Arrays.toString(numeros));
    }

}
